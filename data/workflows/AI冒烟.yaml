name: AI冒烟
note: 一步到底：让 AI 写一句话，机械判据核对。
steps:
- name: 问候
  what: 在 `examples/default/data/artifacts/AI冒烟/问候.md` 里写一行中文问候（就一行，别的什么都不要做）
  executor: AI
  judges:
  - kind: 机械
    note: 问候落在
    spec: contains:examples/default/data/artifacts/AI冒烟/问候.md=你好
